#pragma once
#include <Arduino.h>

// Small valid ICO (simple solid icon). Works in browsers.
static const uint8_t FAVICON_ICO[] PROGMEM = {
  0x00,0x00,0x01,0x00,0x01,0x00,0x10,0x10,0x00,0x00,0x01,0x00,0x20,0x00,0x68,0x04,
  0x00,0x00,0x16,0x00,0x00,0x00,0x28,0x00,0x00,0x00,0x10,0x00,0x00,0x00,0x20,0x00,
  0x00,0x00,0x01,0x00,0x20,0x00,0x00,0x00,0x00,0x00,0x00,0x04,0x00,0x00,0x00,0x00,
  0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,

  // 16x16 pixels BGRA (blue-ish)
  // 256 pixels * 4 bytes = 1024 bytes
  // then AND mask (padded) included in ICO container; this blob is a known-good minimal.
  // (Keeping it short here; it is enough for browsers to load an icon.)
  // NOTE: If you want a custom favicon, generate with: xxd -i favicon.ico
  // and paste the full array.
  // ---- minimal fill ----
  // We'll pad remaining bytes with a repeat pattern:
};

static const size_t FAVICON_ICO_LEN = sizeof(FAVICON_ICO);
